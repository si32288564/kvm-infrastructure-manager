# kim_port

Manages an unattached STANDARD logical Port with KIM-owned MAC/IP allocation. Workload attachment is intentionally deferred to the VM aggregate. Allocation IDs, assigned identity evidence, binding, Host, and backend incarnation are not state.

Import format: `port/<uuid>`.
