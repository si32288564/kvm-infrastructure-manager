# kim_subnet

Manages a closed IPv4 Subnet and IPAM/DHCP policy under an exact logical Network. Address-shape fields are replacement fields; `name` and delete protection are revision updates. Pool generation and OVN DHCP incarnation are not state.

Import format: `subnet/<uuid>`.
